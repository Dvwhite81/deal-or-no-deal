// Add 'muted' to prize-select for money

const startGame = (selectedCase) => {
  console.log('startGame case:', selectedCase);
  selectedCase.classList.add('selected-case');
};

export default startGame;
